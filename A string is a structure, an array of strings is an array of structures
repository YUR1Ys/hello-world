// Возвращает Структуру, заполненную из данными строки таблицы.
//
// 	Параметры:
//  Таблица 		- Табличная часть или таблица значений
//  СтрокаТаблицы 	- Строка табличной части или таблицы значений 
//
// 	Возвращаемое значение:
//  Структура заполненная значениями строки
//
Функция СтрокаТЧСтруктурой(ТаблицаОбъекта, СтрокаТаблицы) Экспорт
	
	ШапкаТЗ = ТаблицаОбъекта.ВыгрузитьКолонки();	
	СтруктураСтроки = Новый Структура;
	
	Для каждого Колонка Из ШапкаТЗ.Колонки Цикл
		СтруктураСтроки.Вставить(Колонка.Имя, СтрокаТаблицы[Колонка.Имя]);
	КонецЦикла;
	
	Возврат СтруктураСтроки;
	
КонецФункции    

// Возвращает Массив структур, заполненный из данными строк таблицы.
//
// 	Параметры:
//  Таблица 		- Табличная часть или таблица значений
//  МассивСтрок 	- МассивСтрок табличной части или таблицы значений 
//
// 	Возвращаемое значение:
//  Массив структуры заполненных значениями массива строк
//
Функция МассивСтрокТЧвМассивСтруктур(ТаблицаОбъекта, МассивСтрок) Экспорт
	
	ШапкаТЗ = ТаблицаОбъекта.ВыгрузитьКолонки();	
	СтруктураСтроки = Новый Структура;
	
	Для Каждого Колонка Из ШапкаТЗ.Колонки Цикл
		СтруктураСтроки.Вставить(Колонка.Имя);
	КонецЦикла;
	
	МассивСтруктур = Новый Массив;
	
	Для Каждого СтрокаИзМассива Из МассивСтрок Цикл
		ЗаполнитьЗначенияСвойств(СтруктураСтроки, СтрокаИзМассива);
		МассивСтруктур.Добавить(СтруктураСтроки);
	КонецЦикла;
		
    Возврат МассивСтруктур;
	
КонецФункции 
