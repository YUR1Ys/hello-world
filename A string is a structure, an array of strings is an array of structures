// Возвращает Структуру, заполненную данными строки таблицы.
//
// Параметры:
// 		Таблица			- Табличная часть или таблица значений
//		СтрокаТаблицы	- Строка табличной части или таблицы значений 
//
// Возвращаемое значение:
//		Структура заполненная значениями строки
//
Функция СтрокаТЧСтруктурой(Таблица, СтрокаТаблицы) Экспорт
	
	ШапкаТЗ = Таблица.ВыгрузитьКолонки();	
	СтруктураСтроки = Новый Структура;
	
	Для каждого Колонка Из ШапкаТЗ.Колонки Цикл
		СтруктураСтроки.Вставить(Колонка.Имя, СтрокаТаблицы[Колонка.Имя]);
	КонецЦикла;
	
	Возврат СтруктураСтроки;
	
КонецФункции    

// Возвращает Массив структур, заполненный из данными коллекции строк таблицы.
//
// 	Параметры:
//  Таблица 		- Табличная часть или таблица значений
//  КоллекцияСтрок 	- МассивСтрок табличной части или таблицы значений 
//
// 	Возвращаемое значение:
//  Массив структур заполненных значениями из коллекции строк
//
Функция МассивСтрокТЧвМассивСтруктур(Таблица, КоллекцияСтрок) Экспорт
	
	ШапкаТЗ = Таблица.ВыгрузитьКолонки();	
	СтруктураСтроки = Новый Структура;
	
	Для Каждого Колонка Из ШапкаТЗ.Колонки Цикл
		СтруктураСтроки.Вставить(Колонка.Имя);
	КонецЦикла;
	
	МассивСтруктур = Новый Массив;
	
	Для Каждого СтрокаИзМассива Из КоллекцияСтрок Цикл
		ЗаполнитьЗначенияСвойств(СтруктураСтроки, СтрокаИзМассива);
		МассивСтруктур.Добавить(СтруктураСтроки);
	КонецЦикла;
		
    Возврат МассивСтруктур;
	
КонецФункции 
