// Возвращает список значений из переданного массива структур
// Имена свойств передаются в качестве соответствующих параметров
&НаКлиентеНаСервереБезКонтекста
Функция МассивСтруктурВСписокЗначений(Знач МассивСтруктур, Значение = "Значение", Представление = "Представление");    
	
	СписокЗначений = Новый СписокЗначений; 
	
	Для Каждого Элемент Из МассивСтруктур Цикл
		СписокЗначений.Добавить(Элемент[Значение]);//, Строка(Элемент[Представление]));
	КонецЦикла;
	
	Возврат СписокЗначений;
	
КонецФункции
